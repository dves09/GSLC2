package gslc2;

//This is the interface that we will be using, as mentioned since this is applied to the abstract class every
//role will have these functions.
public interface playerAction {
	int getHp();
	int getAtk();
	int getDef();
	void skill();
}
